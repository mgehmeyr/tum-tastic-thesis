// Bundled as a package asset (see typst.toml), so this only resolves once
// the package has been installed/published with `assets/` included.
#let tum-tower-image() = image("/assets/TUM_Tower.svg")
